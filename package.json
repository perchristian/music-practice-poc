{
  "name": "piano-practice-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Mock-first piano practice POC for screen recording uploads.",
  "type": "module",
  "scripts": {
    "generate:test-media": "node scripts/generate-phase2a-test-media.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:gui": "playwright test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
